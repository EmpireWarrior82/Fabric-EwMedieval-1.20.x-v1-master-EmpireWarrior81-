package net.empire.ewmedieval.datagen;

/*
import net.empire.ewmedieval.EwMedieval;
import net.empire.ewmedieval.item.ModItems;
import net.empire.ewmedieval.item.ModBannerPatterns;
import net.fabricmc.fabric.api.datagen.v1.FabricDataOutput;
import net.fabricmc.fabric.api.datagen.v1.provider.FabricTagProvider;
import net.minecraft.block.entity.BannerPattern;
import net.minecraft.registry.RegistryKeys;
import net.minecraft.registry.RegistryWrapper;
import net.minecraft.registry.tag.BannerPatternTags;
import net.minecraft.util.Identifier;

import java.util.concurrent.CompletableFuture;

public class ModBannerPatternTagProvider extends FabricTagProvider<BannerPattern> {

    public ModBannerPatternTagProvider(FabricDataOutput output, CompletableFuture<RegistryWrapper.WrapperLookup> registriesFuture) {
        super(output, RegistryKeys.BANNER_PATTERN, registriesFuture);
    }

    @Override
    protected void configure(RegistryWrapper.WrapperLookup arg) {
        getOrCreateTagBuilder(ModItems.CUSTOM_PATTERN_TAG)
                .add(ModBannerPatterns.CUSTOM_PATTERN); // Change back to .add()
    }
}

 */